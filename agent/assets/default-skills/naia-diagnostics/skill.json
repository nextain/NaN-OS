{
	"name": "skill_diagnostics",
	"description": "Gateway diagnostics. Actions: health, status, usage_status, usage_cost, logs_start, logs_stop.",
	"type": "gateway",
	"gatewaySkill": "naia-diagnostics",
	"tier": 0,
	"parameters": {
		"type": "object",
		"properties": {
			"action": {
				"type": "string",
				"description": "Action: health, status, usage_status, usage_cost, logs_start, logs_stop",
				"enum": ["health", "status", "usage_status", "usage_cost", "logs_start", "logs_stop"]
			}
		},
		"required": ["action"]
	}
}
